# Optional dependencies for tabular deep learning on extracted radiomics features.
# The base radiomics extraction and classical ML pipeline only requires requirements.txt.
tensorflow[and-cuda]==2.16.2
